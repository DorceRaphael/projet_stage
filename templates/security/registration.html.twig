{% extends 'base.html.twig' %} 
{% block body %}
    <div class="container mt-4">
        {{ form_start(form) }}
        {{ form_rest(form) }}
        <button>S'inscrire</button>
        {{ form_end(form) }}
    </div>
    {% endblock %}